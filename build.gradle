plugins {
    id 'java'
    id "maven-publish"
}

group = "com.thebombzen"
version = "1.1.0"

publishing {
    publications {
        myLib(MavenPublication) {
            from components.java
        }
    }
    repositories {
        mavenLocal()
    }
}